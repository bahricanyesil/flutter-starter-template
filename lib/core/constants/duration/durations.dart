class Durations {
  static const Duration tooFast = Duration(milliseconds: 200);
  static const Duration fast = Duration(milliseconds: 400);
  static const Duration normal = Duration(milliseconds: 800);
  static const Duration slow = Duration(seconds: 1);
  static const Duration tooSlow = Duration(seconds: 2);
}
